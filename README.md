# CS_203_Project
Command Line Restaurant Viewer. Basically a limited form of something like Yelp (without the comments). Mainly an exercise in using C to implement ArrayList, LinkedList, and BinaryTree.

To run, run `make` from the project root. 
